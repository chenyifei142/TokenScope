更新类型：功能新增

### 新增

- “今日分时”Token 图的分钟悬浮明细新增“本分钟消耗金额”，无可靠费用采样时显示 `--`，零金额显示 `¥0.00`。
- 新增按 Provider 隔离的分钟费用缓存与快照；费用累计增量按有效刷新区间均摊，并保留 Decimal 精度。

### 兼容

- 保持顶部金额、七日趋势、年度活动、底部统计、日周月历史费用口径及外部接口不变。
- 首次采样、跨日、累计值回退或费用字段缺失时只重建费用基线，不补算历史分钟费用。

### 验证

- 已覆盖费用基线、正负增量、跨日、缺失值、零值、缓存清理、Provider 隔离和事务回滚。
- 已覆盖悬浮金额显示、图表边界避让及深浅主题强调色；完整测试集 160 项通过。
